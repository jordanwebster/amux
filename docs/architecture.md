# Architecture

The current architecture is specified in [NEW_ARCHITECTURE.md](NEW_ARCHITECTURE.md).

This file intentionally remains as a pointer for older links. The previous
custom-RPC architecture has been removed; gRPC services, in-band routing events
on `RoutingService.Connect`, and authenticated cloud connectors are the
supported design.
