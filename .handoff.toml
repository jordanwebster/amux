# See templates/handoff.toml in the machinery repo for all keys.

[backend]
provider = "linear"

[repository]
name = "amux"
known = ["amux", "amuxcloud", "handoff"]
theory_homes = ["crates/amux/tests/spec/", "docs/"]

[contracts]
owns = ["protocol", "connect-token"]
