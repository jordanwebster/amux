  ▝▝ ▝▝    ~/.wt/trees/amux/diffs


❯ Please inspect this attached report
  line 2: context
  line 3: reproduction
  line 4: expected
  line 5: observed
  line 6: model input
  line 7: feed output
  line 8: viewer behavior
  line 9: end[Image #15]what is wrong here [Image #16]
  ⎿  [Image #16]

⏺ I received the message text plus one image, the architecture diagram (image 16). No separate report attachment reached
  me, only the nine "line N:" labels in the text and no image 15. If a report file was attached, it did not come
  through.

  What looks wrong in the diagram:

  - Cloud server 2 is orphaned. No host, no mobile app, and no link to cloud server 1. Nothing can reach anything
    through it. Either draw the hosts and clients that use it, or a server-to-server link, or drop it.
  - Application server arrows point the wrong way. A push notification service is something the amux servers call out
    to. As drawn, the app server opens websockets into both amux servers, which makes it a client of them.
  - Mobile app has no path to host3 or host2. It connects only to cloud server 1, and there is no fan-out or routing
    between servers shown. If a mobile app can reach every host, the diagram needs to show how.
  - host2 never reaches the cloud. It hangs off host1 only. If that is intentional relaying, it needs a label saying so,
    and the arrow should start from host1's amux server, not the host box.
  - Label inconsistency. Host-to-cloud links are "tcp connection", mobile-to-cloud is "websocket connection", yet both
    are the same relay protocol carried differently. Pick one level of description.
  - Missing arrowheads. The mobile app and application server lines have arrowheads only at the amux end, and the
    host1-to-cloud line has one at the cloud end, so initiation direction is implied but not stated consistently.

✻ Baked for 27s · done 8:44 PM

─────────────────────────────────────────────────────────────────────────────────────────────────────── attachments-qa ─
❯ [Pasted text #19 +8 lines][Image #20]
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
  Fable 5.1 | ~/.wt/trees/amux/diffs | Context: 95% | 5h: 14% 7d: 22%                                              /rc
  paste again to expand
