    and the arrow should start from host1's amux server, not the host box.
  - Label inconsistency. Host-to-cloud links are "tcp connection", mobile-to-cloud is "websocket connection", yet both
    are the same relay protocol carried differently. Pick one level of description.
  - Missing arrowheads. The mobile app and application server lines have arrowheads only at the amux end, and the
    host1-to-cloud line has one at the cloud end, so initiation direction is implied but not stated consistently.

✻ Baked for 27s · done 8:44 PM

❯ Mixed attachment report
  line 2: context
  line 3: reproduction
  line 4: expected
  line 5: observed
  line 6: model input
  line 7: feed output
  line 8: viewer behavior
  line 9: end[Image #20]what is wrong here [Image #21]
  ⎿  [Image #21]

⏺ Same as last time: one image arrived (image 21, the architecture diagram), and the text report came through inline as
  nine "line N:" labels with "Mixed attachment report" as its first line. No image 20 reached me, so if one was attached
  it was dropped or not rendered into the model input.

  The diagram is identical to the previous one, so the same problems stand:

  - Cloud server 2 is orphaned. No host, client, or server-to-server link reaches it.
  - Application server arrows point the wrong way. Push notifications are something the amux servers call out to, not a
    service that opens websockets into them.
  - Mobile app cannot reach host2 or host3. It connects only to cloud server 1, and no routing between servers is shown.
  - host2 never reaches the cloud. It hangs off host1 with no label saying that is a deliberate relay.
  - Inconsistent labels and arrowheads. Host links say "tcp connection", mobile says "websocket connection", and
    initiation direction is only implied.

✻ Crunched for 7s · done 8:48 PM

─────────────────────────────────────────────────────────────────────────────────────────────────────── attachments-qa ─
❯ check the report for why the first image was dropped
────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
  Fable 5.1 | ~/.wt/trees/amux/diffs | Context: 95% | 5h: 14% 7d: 22%                                              /rc
  ⏵⏵ auto mode on (shift+tab to cycle) · ← for agents
