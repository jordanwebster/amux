# Codex integration close-out

> **CLOSED — 2026-08-18.** The full C.1–C.10 live run completed. C.9's
> automated leg remains process/stream smoke rather than a composer oracle; the
> owner supplied the separate human-inspectable composer witness and accepted
> E3. Plan-created residue was removed and independently verified before E5
> closure.

## What shipped

The corrective and close-out arc made Codex thread publication authoritative
and transport-recoverable, added honest creation recovery plus exact
single-agent removal, typed structured-protocol selection and unknown-agent
degradation, all-build nonfatal invariant handling, measured final-detach raw
PTY teardown, and raw-target preparation outside the agent-registry lock.

Batch C then gave Claude and Codex separate lossless session classifiers,
independent agreement controls at their correct cached/effective-attention
altitudes, authoritative reducer/view/key action gates, and orthogonal
observation-only policy. Its mutation exit proof replays every historical
defect-class instance. Codex startup noise now forms one typed aggregate,
approval amendments have bounded typed labels, and initial persisted resumes
carry the one-shot context-intact feed boundary. E1 removed six asymmetric
serialization-only fields, retained operational `PromptEcho.at`, and aligned
patch-only work classification.

## Durable decisions and evidence

- `docs/CODEX.md` owns Codex behavior, the panic and measured raw-PTY
  decisions, the D9/D10 rulings, and the signed parked/future-work ledger.
- `docs/UI.md` owns the per-layer classification, gate, invariant, readonly,
  and unknown-agent doctrine.
- `closeout/c5-exit-proof.md` and consult #2 hold the ten-row historical
  mutation proof.
- `closeout/e3-live-report.md` holds the real Codex 0.147.0 C-suite evidence
  and keeps the automated C.9 limitation distinct from the owner witness.
- The final close-out summary is the 2026-08-18 top entry in `DEVLOG.md`.

Packet Review was installed concurrently in `b7ed966` after much of the
evidence already existed. Earlier signed briefs/reports preserve their actual
provenance; claims-at-scaffold metadata was not invented retroactively. The
remaining portable-machinery-path issue is filed for its owner rather than
mixed into product work.

## Acceptance and housekeeping

C.9 owner witness passed: post-teardown reattach spawned a fresh codex resume and the composer accepted and echoed input on codex 0.147.0.

This observation closes the user-visible composer claim without rewriting the
automated record: the maintained test still proves only process-group exit,
fresh raw-process creation, structured-stream survival, and zero-turn cleanup.
A future VT100 readiness oracle is optional test automation, not a current
acceptance prerequisite.

All plan-owned G3/E3 capture roots and markers, their correlated scratch
residue, and the C5 orphan process group/root were removed; independent checks
found no correlated amux/Codex process or temporary authentication copy. The
later user-started amux server was preserved as out of scope and had no agents.
Historical owner rollout data and unattributed scratch directories were left
untouched.

Future work starts from `closeout/DISCOVERED.md`, including the raw-composer
automation improvement and the owner-authored Packet Review path P3. The Codex
integration container is closed; those follow-ups belong to the next backlog.
