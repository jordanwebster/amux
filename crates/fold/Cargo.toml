[package]
name = "fold"
version = "0.1.0"
edition.workspace = true
description = "Pure structured-agent observation and transcript folds"
publish = false

[dependencies]
chrono = { workspace = true }
model = { workspace = true }
serde = { workspace = true }

[dev-dependencies]
postcard = { workspace = true }
