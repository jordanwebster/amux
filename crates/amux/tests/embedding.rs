//! No-local-agents embedding acceptance surface.
//!
//! The installation API scenarios are added when that API lands. Keeping the
//! target alive now prevents later work from accidentally sharing the normal
//! workspace feature build.

#[test]
fn embedding_placeholder() {}
