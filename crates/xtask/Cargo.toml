[package]
name = "xtask"
version = "0.1.0"
edition.workspace = true
description = "Developer tasks: `cargo run -p xtask -- codegen` regenerates the committed protobuf code"
publish = false

[dependencies]
tonic-prost-build = { workspace = true }
protoc-bin-vendored = { workspace = true }
